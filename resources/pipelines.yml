resources:
  pipelines:

    platform_observability_pipeline: #Pipeline ID

      name: platform_observability_pipeline # Pipeline Name

      catalog: ${var.catalog}  # Value of Catalog coming from databricks.yml

      schema: ${var.schema} # Value of Schema coming from databricks.yml

      serverless: true

      continuous: false  # Batch vs Streaming

      channel: CURRENT

      libraries:

        - notebook:
            path: ../dlt/platform_observability_pipeline.py

      configuration:

        source_catalog: ${var.catalog}
        source_schema: ${var.schema}



    liquid_clustering_pipeline:

    predictive_optimization_pipeline:

    dlt_health_pipeline:

    governance_pipeline:

